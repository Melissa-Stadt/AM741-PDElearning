x = linspace(0,2, 500);

figure(1)
plot(x, cell_density1(x))


function cell_dense0 = cell_density1(x)
% creates an initial condition cell density over 
% given x values
a = -2000;
b = 1;
c = 0.5;
cell_dense0 = a/(c*sqrt(2*pi))*exp(-0.5*(x-b).^2/c.^2) + a;
end % cell_density